{
  "name": "vision-demo-web",
  "private": true,
  "version": "0.1.0",
  "description": "Vision Demo dashboard — React + Vite (see ../PLAN.md)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {}
}
